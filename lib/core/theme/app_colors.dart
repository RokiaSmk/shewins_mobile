import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFF8E5DAA);
  static const primaryLight = Color(0xFFB388FF);
  static const primaryDark = Color(0xFF6C3D87);

  static const accent = Color(0xFFE88AB6);

  // Backgrounds
  static const background = Color(0xFFFAFAFA);
  static const surface = Colors.white;
  static const card = Colors.white;

  static const softPink = Color(0xFFFDF4F7);
  static const secondary = Color(0xFFF7DCE7);

  // Text
  static const black = Color(0xFF212121);
  static const grey = Color(0xFF757575);
  static const lightGrey = Color(0xFFBDBDBD);

  // Borders
  static const border = Color(0xFFE8E8E8);
  static const divider = Color(0xFFF2F2F2);

  // Status
  static const success = Color(0xFF43A047);
  static const warning = Color(0xFFFF9800);
  static const error = Color(0xFFE53935);
  static const info = Color(0xFF8E6CEF);

  // Others
  static const white = Colors.white;
  static const transparent = Colors.transparent;
}